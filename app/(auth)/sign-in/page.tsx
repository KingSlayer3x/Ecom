import { Metadata } from "next";

export const metadata: Metadata = {
    title: Sign In
}

const SignInPage = () => {
    return ( <div className="w-full max=w-md">Sign In</div> );
}
 
export default SignInPage;